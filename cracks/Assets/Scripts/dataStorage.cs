using UnityEngine;
using System.Collections;

public class dataStorage : MonoBehaviour {



	// called before start
	void Awake () {
//		DontDestroyOnLoad (gameObject);
	}

	void Start(){

//		Object[] found=Object.FindObjectsOfType (typeof(dataStorage));
//		Debug.Log (found.Length);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
